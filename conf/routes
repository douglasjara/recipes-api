# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# An example controller showing a sample home page
GET     /                           controllers.HomeController.index
GET    /explore                     controllers.HomeController.explore
GET    /tutorial                    controllers.HomeController.tutorial

#Recipes
GET     /recipes                      controllers.RecipeController.getRecipes()
POST    /recipe                       controllers.RecipeController.createRecipe()
PATCH   /recipe/:recipeId              controllers.RecipeController.updateRecipe(recipeId: Long)
DELETE  /recipe/:recipeId              controllers.RecipeController.deleteRecipe(recipeId: Long)

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
